// Camera lease machine + badge (Task 4). Stub after Task 2.
// register(ctx) wires requestCameraControl/releaseCameraControl and the pan
// commands (flyTo/jumpTo/fitBounds) revoking the lease. Owns the
// "Camera: <widget name>" badge in #camera-badge.
export function register(ctx) { /* Task 4 implements this */ }
