{
  "name": "anymaps-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test \"tests/**/*.test.js\"",
    "mock": "node dev/mock-server.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
