// Per-widget <style> injection (Task 3). Stub after Task 2.
// register(ctx) wires setStyles. cssText is injected as-is into a per-widget
// <style> element, global scope.
export function register(ctx) { /* Task 3 implements this */ }
