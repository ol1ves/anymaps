# TODO(C): flights widget.

Manifest plus bundle. ADSB.lol external read, series, `@ingestedAt`, poll
every 5 seconds, `?bounds=...&latest=1` for live planes, `?ids=` for the
selected plane's trail. Client-side interpolation between polls. Worked
example: SPEC.md sections 10.3 and 11.6.
