# TODO(C): find-my-friends widget.

Manifest plus bundle. Client write plus client read channels, private, room
token from `POST /widgets/{id}/instances`. Posts geolocation fixes, reads
`?latest=1`. Worked example: SPEC.md sections 8.4 and 9.3.
