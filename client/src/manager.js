// TODO(A): WidgetManager. Owns the registry, worker lifecycle, event routing,
// the map, the DOM, the geolocation watch, and the camera lease. Only code
// that talks to MapLibre directly. Contract: CONTRACTS.md sections 3, 7.10,
// 11.
