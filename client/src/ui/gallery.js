// Gallery UI (Task 6). Stub after Task 2.
// register(ctx) mounts the gallery UI in #gallery: lists published widgets,
// installs and enables them on click.
export function register(ctx) { /* Task 6 implements this */ }
