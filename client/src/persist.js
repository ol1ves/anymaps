// Persist command + deep merge (Task 3). Stub after Task 2.
// register(ctx) wires the persist command and overrides ctx.persistState with
// the real deep-merge + localStorage write. The manager provides a baseline
// ctx.persistState until this module overrides it.
export function register(ctx) { /* Task 3 implements this */ }
