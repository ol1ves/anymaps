# TODO(C): bathrooms widget.

Manifest plus bundle. Overpass `amenity=toilets` over the NYC box, snapshot,
public. The bundle refetches on `viewportChanged` with `?bounds=` and draws
markers with emoji icons. Worked example: SPEC.md section 10.4.
