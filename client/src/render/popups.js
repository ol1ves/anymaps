// Popup arbitration (Task 3). Stub after Task 2.
// register(ctx) wires openPopup/closePopup/setPopupContent. One popup open
// globally; opening a new popup closes the previous across widgets.
export function register(ctx) { /* Task 3 implements this */ }
