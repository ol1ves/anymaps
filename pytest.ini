[pytest]
pythonpath = .
testpaths = shared/tests server/tests agent/tests scripts/tests
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient. is deprecated.*
    ignore:The anyio.abc.BlockingPortal alias is deprecated.*
