// Marker/map-click/viewportChanged events (Task 4). Stub after Task 2.
// register(ctx) wires map click -> mapClick broadcast and moveend ->
// debounced viewportChanged. markerClick is emitted by markers.js; this
// module owns map-level events.
export function register(ctx) { /* Task 4 implements this */ }
