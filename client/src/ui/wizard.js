// Wizard chat panel (Task 7). Stub after Task 2.
// register(ctx) mounts the wizard chat panel in #wizard: relays user prompts
// to the Agent Service, shows clarifying questions, and auto-installs the
// generated widget on done.
export function register(ctx) { /* Task 7 implements this */ }
